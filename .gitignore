.env
service_account.json
logs/
__pycache__/
*.pyc
.claude/settings.local.json

# Design-canvas working files: the .dc.html artboard sources and canvas.json are
# the editable design and are tracked; the seeded canvas is a ~2.4M copy of the
# editor payload regenerated by the design skill on every change, so it isn't.
.design/*.html
!.design/*.dc.html
